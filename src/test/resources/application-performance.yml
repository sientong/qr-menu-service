spring:
  datasource:
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
  
  redis:
    lettuce:
      pool:
        max-active: 50
        max-idle: 10
        min-idle: 5

server:
  tomcat:
    threads:
      max: 200
      min-spare: 20
    max-connections: 10000
    accept-count: 100 